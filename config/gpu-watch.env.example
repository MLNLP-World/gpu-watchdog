# Where to send notifications
# (REQUIRED) Replace with your email address.
TO_EMAIL="you@example.com"

# Trigger rules
# Notify when the number of GPUs detected as "in use" exceeds this threshold.
GPU_LIMIT=2

# Notify when any single process runtime (elapsed time) exceeds this many minutes.
MAX_ETIME_MIN=360

# Anti-spam cooldown:
# For the same alert signature, wait at least N minutes before sending again.
COOLDOWN_MIN=60

